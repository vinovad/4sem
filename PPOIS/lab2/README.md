## Введение

Данная программная система представляет собой приложение для ветеринарной клиники с графическим пользовательским интерфейсом. Система позволяет вести учет пациентов (питомцев), их владельцев, дат посещений и диагнозов.

**Вариант 8** (Ветеринарная клиника) предполагает создание приложения для учета пациентов ветеринарной клиники со следующими полями:
- Имя питомца
- Дата рождения
- Дата последнего приема
- ФИО ветеринара
- Диагноз

**Условия поиска и удаления:**
1. По имени питомца и дате рождения
2. По дате последнего приема и ФИО ветеринара
3. По фразе из диагноза

---

## Архитектура приложения

Приложение построено с использованием шаблона проектирования **Model-View-Controller (MVC)**, который обеспечивает разделение данных, логики и представления. Такая архитектура позволяет независимо модифицировать каждый компонент и упрощает тестирование.

### Структура проекта

```
lab2/
│
├── model/                  # Модель (данные и бизнес-логика)
│   ├── pet.py             # Класс Pet
│   ├── database.py        # Класс PetDatabase
│   └── xml_handler.py     # Класс XMLHandler
│
├── controller/             # Контроллер (обработка действий пользователя)
│   └── app_controller.py  # Класс AppController
│
├── view/                   # Представление (пользовательский интерфейс)
│   ├── main_window.py     # Главное окно
│   ├── widgets/           # Пользовательские виджеты
│   │   └── pagination.py  # Компонент пагинации
│   └── dialogs/           # Диалоговые окна
│       ├── add_dialog.py      # Диалог добавления
│       ├── search_dialog.py   # Диалог поиска
│       └── delete_dialog.py   # Диалог удаления
│
├── data/                   # Директория с данными
│   ├── demo1.xml          # Демо-данные (50 записей)
│   └── demo2.xml          # Демо-данные (50 записей)
│
├── utils/                  # Вспомогательные утилиты
│   └── random_generator.py # Генератор тестовых данных
│
├── config.py               # Конфигурация приложения
└── main.py                 # Точка входа
```

---

## Описание классов модели (Model)

Модель отвечает за хранение данных и реализацию бизнес-логики приложения. Включает три основных класса: `Pet`, `PetDatabase` и `XMLHandler`.

### Pet
**Файл:** `model/pet.py`

Класс предназначен для представления и управления информацией о питомце. Является основной сущностью приложения.

**Как работает:** При создании питомца все его данные инициализируются через конструктор. Класс предоставляет методы для строкового представления объекта, используемые при отладке и отображении.

**Атрибуты:**
- `name`: Имя питомца (строка)
- `birth_date`: Дата рождения питомца (тип `date`)
- `last_visit`: Дата последнего приема (тип `date`)
- `vet_name`: ФИО ветеринара (строка)
- `diagnosis`: Диагноз (строка)

**Методы:**
- `__init__(name, birth_date, last_visit, vet_name, diagnosis)`: Конструктор, инициализирующий все атрибуты питомца.
- `__str__()`: Возвращает строковое представление питомца с отформатированными датами (ДД.ММ.ГГГГ) для отладки.
- `__repr__()`: Возвращает представление объекта для интерпретатора (вызывает `__str__`).

### PetDatabase
**Файл:** `model/database.py`

Класс предназначен для управления коллекцией питомцев, реализации поиска по различным критериям, удаления и постраничного отображения данных.

**Как работает:** Класс хранит список всех питомцев и предоставляет методы для работы с ними. Поиск реализован через фильтрацию списка с помощью list comprehension. Пагинация работает путем возврата среза списка для указанной страницы.

**Атрибуты:**
- `pets`: Список всех питомцев (тип `list[Pet]`)
- `current_page`: Номер текущей страницы (целое число)
- `records_per_page`: Количество записей на странице (целое число, по умолчанию 10)

**Методы:**
- `add_pet(pet)`: Добавляет питомца в базу данных.
- `get_all_pets()`: Возвращает всех питомцев.
- `find_by_name_and_birth(name, birth_date)`: Выполняет поиск по точному совпадению имени и даты рождения (регистронезависимый).
- `find_by_visit_and_vet(last_visit, vet_name)`: Выполняет поиск по точному совпадению даты визита и ФИО ветеринара (регистронезависимый).
- `find_by_diagnosis_phrase(phrase)`: Выполняет поиск по вхождению фразы в диагноз (регистронезависимый).
- `delete_pets(pets_to_delete)`: Удаляет указанных питомцев и возвращает количество удаленных.
- `get_page(page_num)`: Возвращает питомцев для указанной страницы (срез списка).
- `get_total_pages()`: Вычисляет и возвращает общее количество страниц.
- `set_page_size(page_size)`: Устанавливает количество записей на странице.
- `get_current_page()`: Возвращает номер текущей страницы.
- `set_current_page(page_num)`: Устанавливает текущую страницу, возвращает успех операции.

### XMLHandler
**Файл:** `model/xml_handler.py`

Класс предназначен для сохранения и загрузки данных в формате XML с использованием различных парсеров: DOM для записи и SAX для чтения.

**Как работает:** Класс содержит два статических метода. При сохранении создается DOM-документ, в который добавляются элементы для каждого питомца, после чего документ записывается в файл. При загрузке используется SAX-парсер с пользовательским обработчиком, который построчно разбирает XML и создает объекты Pet.

**Атрибуты:** Отсутствуют (все методы статические)

**Методы:**
- `save_to_xml(pets, filename)`: Сохраняет список питомцев в XML-файл с использованием DOM-парсера.
- `load_from_xml(filename)`: Загружает питомцев из XML-файла с использованием SAX-парсера и возвращает список.

**Внутренний класс PetHandler (SAX обработчик):**
- `startElement(name, attrs)`: Обрабатывает открывающие теги, создает словарь для нового питомца при теге `<pet>` и устанавливает флаги для других тегов.
- `characters(content)`: Накапливает текстовое содержимое между тегами.
- `endElement(name)`: Обрабатывает закрывающие теги, сохраняет данные в текущего питомца и создает объект Pet при закрытии тега `<pet>`.

---

## Описание классов контроллера (Controller)

Контроллер обрабатывает действия пользователя, связывает представление с моделью и реализует логику приложения.

### AppController
**Файл:** `controller/app_controller.py`

Главный контроллер приложения, управляющий всеми операциями и взаимодействием между моделью и представлением.

**Как работает:** Контроллер хранит ссылки на модель (`PetDatabase`) и представление (`MainWindow`). При вызове методов из представления (например, при нажатии кнопок), контроллер выполняет необходимые операции с моделью, обрабатывает ошибки и обновляет представление.

**Атрибуты:**
- `view`: Ссылка на главное окно приложения (тип `MainWindow`)
- `database`: Экземпляр базы данных питомцев (тип `PetDatabase`)
- `current_file`: Путь к текущему файлу для сохранения/загрузки (строка или None)

**Методы:**
- `__init__(view)`: Инициализирует контроллер с пустой базой данных и без представления.
- `load_demo_data()`: Загружает демо-данные из файлов `data/demo1.xml` и `data/demo2.xml`.
- `update_view()`: Обновляет таблицу и информацию о пагинации в представлении.
- `show_add_dialog()`: Создает и показывает диалог добавления питомца.
- `show_search_dialog()`: Создает и показывает диалог поиска.
- `show_delete_dialog()`: Создает и показывает диалог удаления.
- `add_pet(pet_data)`: Добавляет нового питомца, выполняет валидацию дат и проверку логики (дата визита не может быть раньше даты рождения).
- `search_by_name_and_birth(name, birth_date_str)`: Выполняет поиск по имени и дате рождения, обрабатывает ошибки парсинга даты.
- `search_by_visit_and_vet(last_visit_str, vet_name)`: Выполняет поиск по дате визита и ветеринару.
- `search_by_diagnosis_phrase(phrase)`: Выполняет поиск по фразе в диагнозе.
- `delete_pets(pets_to_delete)`: Удаляет указанных питомцев и обновляет представление.
- `load_from_xml()`: Открывает диалог выбора файла и загружает данные из XML.
- `save_to_xml()`: Открывает диалог сохранения файла и сохраняет данные в XML.
- `change_page(page_num)`: Изменяет текущую страницу и обновляет представление.
- `change_page_size(page_size)`: Изменяет количество записей на странице.
- `_parse_date(date_str)`: Вспомогательный метод для парсинга строки в объект `date` (поддерживает форматы ДД.ММ.ГГГГ и ГГГГ-ММ-ДД).

---

## Описание классов представления (View)

Представление отвечает за отображение данных и взаимодействие с пользователем. Включает главное окно, компонент пагинации и три диалоговых окна.

### MainWindow
**Файл:** `view/main_window.py`

Главное окно приложения, содержащее таблицу с записями, панель инструментов и меню.

**Как работает:** Окно создается как наследник `tk.Tk`. При инициализации создается таблица и компонент пагинации, но меню и панель инструментов создаются только после установки контроллера методом `set_controller`. Таблица обновляется методом `update_table`, который очищает старые данные и добавляет новые.

**Наследование:** `tk.Tk`

**Атрибуты:**
- `controller`: Ссылка на контроллер (тип `AppController`)
- `tree`: Таблица для отображения записей (тип `ttk.Treeview`)
- `pagination`: Компонент постраничной навигации (тип `Pagination`)

**Методы:**
- `__init__(controller)`: Создает окно, таблицу и пагинацию.
- `set_controller(controller)`: Устанавливает контроллер и создает меню и панель инструментов.
- `_create_table()`: Создает таблицу с пятью колонками (имя, дата рождения, дата визита, ветеринар, диагноз) и полосами прокрутки.
- `_create_menu()`: Создает главное меню с пунктами "Файл" (загрузка, сохранение, выход) и "Операции" (добавить, поиск, удалить).
- `_create_toolbar()`: Создает панель инструментов с кнопками, дублирующими команды меню.
- `update_table(pets)`: Очищает таблицу и заполняет ее данными из списка питомцев (форматирует даты в ДД.ММ.ГГГГ).

### Pagination
**Файл:** `view/widgets/pagination.py`

Компонент постраничной навигации, обеспечивающий управление страницами.

**Как работает:** Компонент создает набор элементов управления: кнопки для навигации, поля для ввода номера страницы и размера страницы, метки с информацией. При изменении значений вызываются соответствующие методы контроллера.

**Наследование:** `ttk.Frame`

**Атрибуты:**
- `controller`: Ссылка на контроллер
- `current_page`: Переменная для хранения текущей страницы (тип `tk.IntVar`)
- `page_size`: Переменная для хранения размера страницы (тип `tk.IntVar`)
- `total_pages_label`: Метка для отображения общего числа страниц
- `total_records_label`: Метка для отображения общего числа записей

**Методы:**
- `__init__(parent, controller)`: Создает компонент и все элементы управления.
- `_create_widgets()`: Создает кнопки навигации, поля ввода и информационные метки.
- `update_pagination(current_page, total_pages, total_records)`: Обновляет отображаемую информацию.
- `_go_to_page()`: Обрабатывает ввод номера страницы в поле (по нажатию Enter).
- `_change_page_size()`: Обрабатывает изменение размера страницы (по нажатию Enter).

### AddPetDialog
**Файл:** `view/dialogs/add_dialog.py`

Диалоговое окно для добавления нового питомца.

**Как работает:** Окно создается как модальный диалог (`transient` и `grab_set`), блокирующий взаимодействие с главным окном до закрытия. Содержит поля для ввода всех атрибутов питомца. Для ввода дат используется специализированный компонент `DateEntry` с календарем.

**Наследование:** `tk.Toplevel`

**Атрибуты:**
- `controller`: Ссылка на контроллер
- `name_entry`: Поле ввода имени питомца
- `birth_date_entry`: Поле ввода даты рождения (тип `DateEntry`)
- `last_visit_entry`: Поле ввода даты последнего приема (тип `DateEntry`)
- `vet_name_entry`: Поле ввода ФИО ветеринара
- `diagnosis_entry`: Поле ввода диагноза

**Методы:**
- `__init__(parent, controller)`: Создает диалог, центрирует его и создает форму.
- `_center_window()`: Центрирует окно относительно родительского.
- `_create_form()`: Создает форму с полями ввода и кнопками "Добавить" и "Отмена".
- `_add_pet()`: Собирает данные из полей, проверяет заполнение всех полей и передает данные контроллеру.

### SearchDialog
**Файл:** `view/dialogs/search_dialog.py`

Диалоговое окно для поиска питомцев по различным критериям.

**Как работает:** Диалог содержит три вкладки (поиск по имени+дате, по визиту+ветеринару, по диагнозу). На каждой вкладке есть форма для ввода критериев и таблица для отображения результатов. При нажатии кнопки "Найти" вызывается соответствующий метод контроллера, результаты отображаются в таблице.

**Наследование:** `tk.Toplevel`

**Атрибуты:**
- `controller`: Ссылка на контроллер
- `name_birth_tree`: Таблица для результатов поиска по имени и дате
- `visit_vet_tree`: Таблица для результатов поиска по визиту и ветеринару
- `diagnosis_tree`: Таблица для результатов поиска по диагнозу
- Поля ввода для каждого критерия поиска

**Методы:**
- `__init__(parent, controller)`: Создает диалог, центрирует его и создает вкладки.
- `_create_name_birth_tab(parent)`: Создает вкладку поиска по имени и дате рождения.
- `_create_visit_vet_tab(parent)`: Создает вкладку поиска по дате визита и ветеринару.
- `_create_diagnosis_tab(parent)`: Создает вкладку поиска по фразе в диагнозе.
- `_search_by_name_and_birth()`: Выполняет поиск по имени и дате, отображает результаты.
- `_search_by_visit_and_vet()`: Выполняет поиск по визиту и ветеринару.
- `_search_by_diagnosis()`: Выполняет поиск по диагнозу.
- `_display_results(tree, results)`: Отображает результаты поиска в указанной таблице.

### DeleteDialog
**Файл:** `view/dialogs/delete_dialog.py`

Диалоговое окно для удаления питомцев по различным критериям.

**Как работает:** Аналогично диалогу поиска, содержит три вкладки. Отличие в том, что после поиска пользователь может выбрать записи в таблице и нажать кнопку "Удалить выбранные записи". Перед удалением запрашивается подтверждение с указанием количества записей.

**Наследование:** `tk.Toplevel`

**Атрибуты:**
- `controller`: Ссылка на контроллер
- Три таблицы для отображения найденных записей (аналогично SearchDialog)
- Поля ввода для каждого критерия удаления

**Методы:**
- `__init__(parent, controller)`: Создает диалог и вкладки.
- `_create_name_birth_tab(parent)`: Создает вкладку удаления по имени и дате рождения.
- `_create_visit_vet_tab(parent)`: Создает вкладку удаления по визиту и ветеринару.
- `_create_diagnosis_tab(parent)`: Создает вкладку удаления по диагнозу.
- `_search_by_name_and_birth()`: Выполняет поиск по имени и дате.
- `_search_by_visit_and_vet()`: Выполняет поиск по визиту и ветеринару.
- `_search_by_diagnosis()`: Выполняет поиск по диагнозу.
- `_display_results(tree, results)`: Отображает результаты поиска.
- `_delete_selected_name_birth()`: Удаляет выбранные записи из вкладки по имени и дате.
- `_delete_selected_visit_vet()`: Удаляет выбранные записи из вкладки по визиту и ветеринару.
- `_delete_selected_diagnosis()`: Удаляет выбранные записи из вкладки по диагнозу.

---

## Реализация требований задания

### Требование 1: Архитектура MVC
**Реализация:** Приложение полностью построено по шаблону MVC. Модель (`model/`) содержит классы для данных и бизнес-логики, контроллер (`controller/`) обрабатывает действия пользователя, представление (`view/`) отвечает за интерфейс.

### Требование 2: Добавление записей через диалог
**Реализация:** Диалог `AddPetDialog` содержит форму с пятью полями. Для ввода дат используется компонент `DateEntry` с календарем. При добавлении выполняется валидация заполнения всех полей и проверка логики дат (дата визита не может быть раньше даты рождения).

### Требование 3: Поиск записей в отдельном диалоге
**Реализация:** Диалог `SearchDialog` содержит три вкладки, соответствующие трем условиям поиска варианта 8. Результаты поиска отображаются в таблице внутри того же диалога.

### Требование 4: Удаление записей в отдельном диалоге
**Реализация:** Диалог `DeleteDialog` также содержит три вкладки. Процесс удаления двухэтапный: сначала поиск по критериям, затем выбор записей и удаление.

### Требование 5: Информирование о результатах удаления
**Реализация:** При удалении пользователь получает несколько сообщений:
- Если записи не найдены: "Записи не найдены"
- Если не выбраны записи: "Нет выбранных записей для удаления"
- Подтверждение с количеством: "Вы действительно хотите удалить N записей?"
- После удаления: "Успешно удалено N записей"

### Требование 6: Отображение всего массива в главном окне
**Реализация:** Главное окно `MainWindow` содержит таблицу `Treeview` с пятью колонками. Данные отображаются с постраничной навигацией.

### Требование 7: Сохранение и загрузка XML (DOM/SAX)
**Реализация:** Класс `XMLHandler` реализует:
- Сохранение с использованием DOM-парсера (`xml.dom.minidom`)
- Загрузку с использованием SAX-парсера (`xml.sax` с пользовательским обработчиком)
- Стандартные диалоги выбора файла через `filedialog`

### Требование 8: Правильные типы данных
**Реализация:** 
- Даты хранятся как `datetime.date` в модели
- Для ввода дат используется `DateEntry` с календарем
- При отображении даты форматируются в ДД.ММ.ГГГГ

### Требование 9: Постраничная навигация
**Реализация:** Компонент `Pagination` предоставляет:
- Кнопки "Первая", "Предыдущая", "Следующая", "Последняя"
- Поле для ввода номера страницы
- Отображение текущей страницы и общего числа страниц
- Возможность изменения количества записей на странице
- Отображение общего числа записей

### Требование 10: Демо-данные (50+ уникальных записей)
**Реализация:** 
- Сгенерировано 100 уникальных записей в двух файлах (`demo1.xml`, `demo2.xml`)
- Использованы реальные имена питомцев, ФИО ветеринаров и осмысленные диагнозы
- Даты сгенерированы с учетом логики (дата визита после даты рождения)

---

## Сценарии демонстрации работы

### Сценарий 1: Запуск приложения и загрузка демо-данных

**Действия пользователя:**
1. Запустить приложение командой `python main.py`

**Ожидаемый результат:**
- Открывается главное окно с таблицей
- Автоматически загружаются демо-данные (100 записей)
- В таблице отображаются первые 10 записей
- В пагинации показано: текущая страница 1, всего страниц 10, всего записей 100

![](4sem/PPOIS/lab2/screenshot/1.1.png)

![](4sem/PPOIS/lab2/screenshot/1.2.png)

---

### Сценарий 2: Добавление нового питомца

**Действия пользователя:**
1. Нажать кнопку "Добавить" на панели инструментов
2. В открывшемся диалоге заполнить все поля:
   - Имя питомца: "Шарик"
   - Дата рождения: выбрать 15.05.2020 (через календарь)
   - Дата последнего приема: выбрать 10.10.2023
   - ФИО ветеринара: "Сидоров Иван Петрович"
   - Диагноз: "Отит, назначены капли"
3. Нажать кнопку "Добавить"

**Ожидаемый результат:**
- Диалог закрывается
- Появляется сообщение "Питомец успешно добавлен"
- Таблица обновляется, новая запись отображается на текущей странице (или создается новая страница)
- Общее количество записей увеличивается до 101

![](4sem/PPOIS/lab2/screenshot/2.1.png)

![](4sem/PPOIS/lab2/screenshot/2.2.png)

![](4sem/PPOIS/lab2/screenshot/2.3.png)

---

### Сценарий 3: Поиск питомца по имени и дате рождения

**Действия пользователя:**
1. Нажать кнопку "Поиск" на панели инструментов
2. Выбрать вкладку "По имени и дате рождения"
3. Ввести имя: "Бруно"
4. Выбрать дату рождения: 10.04.2021
5. Нажать кнопку "Найти"

**Ожидаемый результат:**
- В таблице результатов отображаются все питомцы с именем "Бруно" и датой рождения 10.04.2021
- Если таких записей нет, появляется сообщение "Записи не найдены"

![](4sem/PPOIS/lab2/screenshot/3.1.png)

![](4sem/PPOIS/lab2/screenshot/3.2.png)

---

### Сценарий 4: Поиск питомца по фразе в диагнозе

**Действия пользователя:**
1. Открыть диалог поиска
2. Выбрать вкладку "По диагнозу"
3. Ввести фразу: "отит"
4. Нажать "Найти"

**Ожидаемый результат:**
- В таблице отображаются все питомцы, в диагнозе которых встречается слово "отит" (регистронезависимо)
- Например, запись "Шарик" с диагнозом "Отит, назначены капли" будет найдена

![](4sem/PPOIS/lab2/screenshot/4.1.png)

---

### Сценарий 5: Удаление записей по критериям

**Действия пользователя:**
1. Нажать кнопку "Удалить" на панели инструментов
2. Выбрать вкладку "По имени и дате рождения"
3. Ввести имя: "Бруно"
4. Выбрать дату рождения: 10.04.2021
5. Нажать "Найти"
6. В таблице результатов выбрать одну или несколько записей
7. Нажать "Удалить выбранные записи"

**Ожидаемый результат:**
- Появляется запрос подтверждения: "Вы действительно хотите удалить X записей?"
- После подтверждения появляется сообщение "Успешно удалено X записей"
- Таблица результатов обновляется (записи исчезают)
- Главное окно обновляется, удаленные записи больше не отображаются

![](4sem/PPOIS/lab2/screenshot/5.1.png)

![](4sem/PPOIS/lab2/screenshot/5.2.png)

![](4sem/PPOIS/lab2/screenshot/5.3.png)

![](4sem/PPOIS/lab2/screenshot/5.4.png)

---

### Сценарий 6: Попытка удаления без выбора записей

**Действия пользователя:**
1. Открыть диалог удаления
2. Найти записи по каким-либо критериям
3. Не выбирая записей, нажать "Удалить выбранные записи"

**Ожидаемый результат:**
- Появляется сообщение "Нет выбранных записей для удаления"
- Удаления не происходит

![](4sem/PPOIS/lab2/screenshot/6.1.png)

---

### Сценарий 7: Попытка удаления несуществующих записей

**Действия пользователя:**
1. Открыть диалог удаления
2. Ввести имя несуществующего питомца, например "Несуществующий"
3. Нажать "Найти"

**Ожидаемый результат:**
- Появляется сообщение "Записи не найдены"
- Таблица результатов остается пустой

![](4sem/PPOIS/lab2/screenshot/7.1.png)

---

### Сценарий 8: Сохранение данных в XML-файл

**Действия пользователя:**
1. В главном окне нажать кнопку "Сохранить" или выбрать меню "Файл" → "Сохранить в XML"
2. В диалоге сохранения выбрать папку и ввести имя файла, например "my_pets.xml"
3. Нажать "Сохранить"

**Ожидаемый результат:**
- Появляется сообщение "Данные успешно сохранены"
- В указанной папке создается XML-файл со всеми записями

![](4sem/PPOIS/lab2/screenshot/8.1.png)

![](4sem/PPOIS/lab2/screenshot/8.2.png)

![](4sem/PPOIS/lab2/screenshot/8.3.png)

---

### Сценарий 9: Загрузка данных из XML-файла

**Действия пользователя:**
1. В главном окне нажать кнопку "Загрузить" или выбрать меню "Файл" → "Загрузить из XML"
2. В диалоге выбора файла найти и выбрать ранее сохраненный файл "my_pets.xml"
3. Нажать "Открыть"

**Ожидаемый результат:**
- Появляется сообщение "Загружено X записей из my_pets.xml"
- Таблица обновляется, отображая загруженные записи
- Общее количество записей увеличивается на количество загруженных

![](4sem/PPOIS/lab2/screenshot/9.1.png)

![](4sem/PPOIS/lab2/screenshot/9.2.png)

---

### Сценарий 10: Навигация по страницам

**Действия пользователя:**
1. В главном окне нажать кнопку "Следующая >"
2. Нажать кнопку "Последняя >>"
3. В поле "Страница" ввести номер, например "5", и нажать Enter
4. В поле "Записей на странице" ввести "20" и нажать Enter

**Ожидаемый результат:**
- При нажатии "Следующая" отображается следующая страница
- При нажатии "Последняя" отображается последняя страница
- При вводе номера страницы происходит переход на указанную страницу
- При изменении размера страницы таблица обновляется, показывая новое количество записей
- Появляется сообщение "Количество записей на странице изменено на 20"
- Обновляется информация о количестве страниц

![](4sem/PPOIS/lab2/screenshot/10.1.png)

![](4sem/PPOIS/lab2/screenshot/10.2.png)

![](4sem/PPOIS/lab2/screenshot/10.3.png)

![](4sem/PPOIS/lab2/screenshot/10.4.png)

![](4sem/PPOIS/lab2/screenshot/10.5.png)

---

## Заключение

В ходе выполнения лабораторной работы было разработано оконное приложение "Ветеринарная клиника" с графическим интерфейсом на языке Python.

**Результаты работы:**
1. Реализована архитектура Model-View-Controller, обеспечивающая разделение данных, логики и представления.
2. Создано главное окно с таблицей для отображения записей, меню и панелью инструментов.
3. Разработаны три диалоговых окна для добавления, поиска и удаления записей.
4. Реализованы три условия поиска и удаления согласно варианту 8:
   - По имени питомца и дате рождения
   - По дате последнего приема и ФИО ветеринара
   - По фразе из диагноза
5. Обеспечено информирование пользователя о результатах операций.
6. Реализована постраничная навигация с возможностью изменения размера страницы.
7. Реализовано сохранение и загрузка данных в формате XML с использованием DOM (запись) и SAX (чтение) парсеров.
8. Сгенерированы демо-данные: 100 уникальных осмысленных записей в двух файлах.

**Использованные технологии:**
- Python 3.14
- Tkinter для графического интерфейса
- tkcalendar для ввода дат
- xml.dom.minidom для записи XML
- xml.sax для чтения XML
- unittest для модульного тестирования
